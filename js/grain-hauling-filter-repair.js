/* FarmVista — hauling filter repair disabled.
   Reverted Sept. 15, 2026 filter experiments. The existing hauling workspace owns
   rendering and filtering again; this file intentionally performs no DOM work. */
(() => {
  'use strict';
})();
