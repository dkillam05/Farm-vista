# DO NOT MERGE YET

`major-js-restructure-cleanup` is an active restructuring branch.

Folder architecture is being established first, followed by relocation of every JavaScript file, migration of every consumer/reference, deletion of old duplicate paths, and static verification.

This marker should be removed only when the branch is ready for the user's GitHub folder-layout review. Runtime testing follows that review. `main` is intentionally untouched during this operation.
