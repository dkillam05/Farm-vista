# `/js` Root Exceptions

The target is a clean `/js` root. Documentation files may remain at `/js` root because they describe the organization itself.

Runtime JavaScript should remain directly under `/js` only if there is a concrete bootstrap/loading reason that makes nesting unsafe or impractical. Any such runtime exception must be listed here with its reason before the restructure is marked complete.

## Runtime exceptions

_None approved yet._
